{
  "name": "playwright-dagger-repro",
  "version": "1.0.0",
  "description": "Minimal reproduction repo for running Playwright UI mode from Dagger",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui --ui-host=0.0.0.0 --ui-port=48002"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0",
    "@dagger.io/dagger": "^0.15.1"
  }
}
